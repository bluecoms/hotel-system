# -*- coding: utf-8 -*-
# ============================================================================
# File      : app/db/base.py
# Version   : 2025-10-31 · v3.6 (SSOT Stable)
# Purpose   : Declarative Base 초기화 및 모델 등록
# ----------------------------------------------------------------------------
# 목적:
#   • SQLAlchemy Declarative Base를 단일 진입점으로 제공
#   • Alembic / ORM 간 메타데이터 동기화를 위한 공용 베이스 유지
#   • app/models 패키지 내 모든 ORM 모델을 import 하여
#     Base.metadata 에 자동 등록 (사이드이펙트 import)
# ----------------------------------------------------------------------------
# 사용 예:
#   from app.db.base import Base
#   Base.metadata.create_all(bind=engine)
# ----------------------------------------------------------------------------
# 구성 규칙:
#   ✅ Base는 app/db/base_class.py 에서 선언
#   ✅ models 패키지를 import 하면 모든 ORM 모델이 메타데이터에 등록됨
# ============================================================================

from .base_class import Base

# ─────────────────────────────────────────────
# 모델 등록 (사이드이펙트 import)
# ─────────────────────────────────────────────
from app import models  # noqa: F401  # import side-effect: 모델 메타데이터 등록

# ─────────────────────────────────────────────
# 공개 심볼
# ─────────────────────────────────────────────
__all__ = ["Base"]
